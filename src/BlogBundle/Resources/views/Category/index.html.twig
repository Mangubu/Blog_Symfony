{% extends 'BlogBundle::layout.html.twig' %}

{% block body %}
<div class="categories">
  {% for category in categories %}
    <a class="waves-effect waves-light btn blue darken-2" href="{{ path('blog_posts', {categoryId: category.id}) }}">{{ category.name }}</a>
  {% endfor %}
</div>
{% endblock %}
